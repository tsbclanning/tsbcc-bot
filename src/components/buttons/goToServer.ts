import type { ButtonInteraction } from 'discord.js';

// Link button — no action needed
export async function handleGoToServerButton(interaction: ButtonInteraction): Promise<void> {
  // This is a link button, no handler needed
}
